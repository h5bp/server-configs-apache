# htaccess_mock_valid.conf
# Partials configuration file for test_userbuild.sh
#
# VALID mock configuration, using all possible (and only valid) keywords.

title   "The title"
enable  "test/mocks/a.conf"
disable "test/mocks/b.conf"
omit    "test/mocks/c.conf"

